{{- if .Values.keycloak.enabled }}
---
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.keycloak.namespace }}
  labels:
    {{- include "keycloak.labels" . | nindent 4 }}
    argocd.argoproj.io/managed-by: {{ .Values.argocd.namespace | default "openshift-gitops" }}
  annotations:
    argocd.argoproj.io/sync-wave: {{ .Values.keycloak.syncWaves.namespace | quote }}
{{- end }}
